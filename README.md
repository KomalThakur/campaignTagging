# campaignTagging


$ npm uninstall -g @angular/cli
$ npm cache clean
$ npm install -g @angular/cli@6.1.1
Install node > 8.11

cd Frontend
$ npm install
$ ng serve

cd Backend 
$ npm install
$ npm start

